# Core ML/Data
torch>=2.0.0
transformers>=4.35.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# Genomics-specific
cyvcf2>=0.30.0  # Fast VCF parsing
pysam>=0.21.0  # BAM/VCF handling
biopython>=1.81  # Sequence analysis

# Data storage (local-friendly)
pyarrow>=13.0.0  # Parquet files
duckdb>=0.9.0  # SQL on Parquet, no server
polars>=0.19.0  # Fast dataframes (Rust-based)

# API (generates OpenAPI spec)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.4.0

# Utilities
requests>=2.31.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# Cloud (GCP) - MARKED: WON'T RUN LOCALLY
# google-cloud-storage>=2.10.0
# google-cloud-bigquery>=3.13.0

# Optional: Model serving
# bentoml>=1.1.0  # For production model serving
